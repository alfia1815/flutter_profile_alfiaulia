name: widget_kompleks
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Tambahkan package lain di sini kalau dibutuhkan
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/aqua.jpeg
    - assets/images/lemineral.jpeg
    - assets/images/cleo.jpg
    - assets/images/nestle.jpg